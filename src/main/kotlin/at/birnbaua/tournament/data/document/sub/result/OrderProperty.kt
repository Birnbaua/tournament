package at.birnbaua.tournament.data.document.sub.result

enum class OrderProperty {
    EXTERNAL_RANK,
    INTERNAL_RANK,
    POINTS,
    GAME_POINTS,
    OWN_GAME_POINTS,
    OPPONENT_GAME_POINTS,
    DIRECT_MATCHES,
    DIRECT_MATCHES_WITH_HOME_GAMES,
    TEAM_NO,
    WINS,
    DRAWS,
    DEFEATS,
    INTERNAL_CORRECTION,
    EXTERNAL_CORRECTION
}